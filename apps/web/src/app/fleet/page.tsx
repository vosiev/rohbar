"use client";
import { useState } from "react";
import { Truck, UsersRound, Plus, Settings2, X, Save } from "lucide-react";
import { Button, Card, Field, PageHeader, inputClass } from "@/components/rohbar-ui";
const initial=[{name:"MAN TGX",type:"Тент",capacity:"20 т",volume:"86 м³",status:"Доступен"},{name:"Volvo FH",type:"Рефрижератор",capacity:"10 т",volume:"55 м³",status:"В рейсе"},{name:"КАМАЗ 54901",type:"Тент",capacity:"20 т",volume:"82 м³",status:"Доступен"}];
export default function Fleet(){const [vehicles,setVehicles]=useState(initial);const [open,setOpen]=useState(false);const [name,setName]=useState("");const [type,setType]=useState("Тент");function add(e:React.FormEvent){e.preventDefault();if(!name.trim())return;setVehicles(v=>[...v,{name:name.trim(),type,capacity:"20 т",volume:"—",status:"Доступен"}]);setName("");setOpen(false);}return <div className="mx-auto max-w-7xl"><PageHeader eyebrow="RohBar · перевозчик" title="Автопарк" description="Транспорт и водители для автоматического подбора заявок." action={<Button onClick={()=>setOpen(true)}><Plus size={17}/>Добавить транспорт</Button>}/><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{vehicles.map(v=><Card key={v.name}><div className="flex items-center justify-between"><span className="grid size-11 place-items-center rounded-2xl bg-slate-50"><Truck/></span><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${v.status==="Доступен"?"bg-emerald-50 text-emerald-700":"bg-blue-50 text-blue-700"}`}>{v.status}</span></div><h2 className="mt-5 text-lg font-black">{v.name}</h2><p className="mt-1 text-sm text-slate-500">{v.type} · {v.capacity} · {v.volume}</p><Button variant="secondary" className="mt-5 w-full"><Settings2 size={16}/>Управлять</Button></Card>)}</div><Card className="mt-6"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-2xl bg-teal-50 text-teal-700"><UsersRound/></span><div><h2 className="font-black">Водители и команда</h2><p className="mt-1 text-sm text-slate-500">Команда и назначения готовы к подключению к backend.</p></div></div></Card>{open&&<div className="fixed inset-0 z-50 grid place-items-end bg-slate-950/40 p-3 sm:place-items-center"><div className="w-full max-w-md rounded-3xl bg-white p-6 shadow-2xl"><div className="flex items-center justify-between"><h2 className="text-xl font-black">Добавить транспорт</h2><button onClick={()=>setOpen(false)} aria-label="Закрыть"><X/></button></div><form onSubmit={add} className="mt-6 space-y-5"><Field label="Модель" required><input className={inputClass} required value={name} onChange={e=>setName(e.target.value)} placeholder="Например, MAN TGX"/></Field><Field label="Тип кузова" required><select className={inputClass} value={type} onChange={e=>setType(e.target.value)}><option>Тент</option><option>Рефрижератор</option><option>Цельнометаллический</option><option>Бортовой</option></select></Field><Button type="submit" className="w-full"><Save size={17}/>Сохранить</Button></form></div></div>}</div>}
