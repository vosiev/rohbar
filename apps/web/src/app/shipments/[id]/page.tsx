import Link from "next/link";
import { ArrowLeft, MapPin, Package, Truck, UserRound } from "lucide-react";
export default async function ShipmentDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;return <div className="mx-auto max-w-4xl"><Link href="/shipments" className="inline-flex items-center gap-2 text-sm font-bold text-gray-500"><ArrowLeft size={16}/>Назад к перевозкам</Link><div className="mt-5 rounded-3xl border bg-white p-5 sm:p-8"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><span className="rounded-full bg-teal-50 px-3 py-1 text-xs font-bold text-teal-800">Есть предложения</span><p className="mt-3 text-sm text-gray-400">{id}</p><h1 className="mt-1 text-3xl font-black">Москва → Казань</h1></div><p className="text-2xl font-black">85 000 ₽</p></div><div className="mt-8 grid gap-4 sm:grid-cols-2"><Info icon={<MapPin/>} label="Маршрут" value="Москва → Казань"/><Info icon={<Package/>} label="Груз" value="Строительные материалы · 20 т"/><Info icon={<Truck/>} label="Транспорт" value="Тент"/><Info icon={<UserRound/>} label="Заказчик" value="Вектор Логистик"/></div><div className="mt-8 rounded-2xl bg-gray-50 p-5"><p className="font-bold">Статус перевозки</p><div className="mt-5 grid gap-4 sm:grid-cols-5">{["Опубликована","Предложения","Перевозчик выбран","В пути","Доставлена"].map((x,i)=><div key={x}><div className={`grid size-8 place-items-center rounded-full text-sm font-bold ${i<2?"bg-teal-700 text-white":"bg-white text-gray-400"}`}>{i+1}</div><p className="mt-2 text-xs font-semibold text-gray-500">{x}</p></div>)}</div></div></div></div>}
function Info({icon,label,value}:{icon:React.ReactNode;label:string;value:string}){return <div className="rounded-2xl border p-4"><div className="flex items-center gap-2 text-teal-700">{icon}<span className="text-xs font-bold uppercase tracking-wide text-gray-400">{label}</span></div><p className="mt-3 font-bold">{value}</p></div>}
